1)Class binding
2)Style binding
3)Angular ngFor trackBy:
    Increases performance as angular now tracks table using column value which when not changed does not update dom. By default angular uses references to update all rows in dom if any row is changed.
